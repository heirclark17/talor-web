/**
 * Resume Builder - Premium AI-Powered Builder
 *
 * 7-step wizard with split-screen live preview,
 * real-time quality scoring, and 3 AI features.
 */

import BuilderLayout from '../components/builder/BuilderLayout';

export default function ResumeBuilder() {
  return <BuilderLayout />;
}
